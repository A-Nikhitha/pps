#include <stdio.h>
int main()
{
        int num;
        printf("enter the inpu");
        scanf("%d",&num);
        if(num>0)
        printf("the number=%d is positive",num);
        else
        printf("the number=%d is negative",num);
}